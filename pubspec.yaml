name: cam_watcher
description: A simple MJEPG-Stream watcher.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_form_builder: ^7.0.0
  flutter_mjpeg: ^2.0.1
  get: ^4.6.1
  getwidget: ^2.0.4
  hive: ^2.0.5
  hive_flutter: ^1.1.0
  path_provider: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.4
  flutter_lints: ^1.0.4
  mockito: ^5.0.17

flutter:
  uses-material-design: true
